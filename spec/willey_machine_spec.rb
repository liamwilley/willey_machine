require 'spec_helper'

describe WilleyMachine do
  it 'has a version number' do
    expect(WilleyMachine::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
